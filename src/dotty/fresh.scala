package dotty

/**
  * fresh annotation
  * Fresh methods and expressions are this-polymorphic in their receiver mutabilities, but return mutable references by default.
  */
class fresh extends scala.annotation.Annotation
